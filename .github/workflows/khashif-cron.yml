name: Khashif Hourly

on:
  schedule:
    - cron: '0 * * * *'

jobs:
  wake:
    runs-on: ubuntu-latest
    steps:
      - name: Wake Khashif
        run: |
          curl -X POST \
            -H "X-Khashif-Key: khashif2026" \
            https://khashif.onrender.com/task
